#!/data/data/com.termux/files/usr/bin/sh
# Gradle wrapper stub — GitHub Actions использует свой gradle
# через gradle/actions/setup-gradle, этот файл нужен только
# чтобы workflow не падал на chmod +x gradlew
echo "Gradle wrapper: use GitHub Actions setup-gradle instead"
exit 0
